{{#each pendiente}}

<div class="container p-4">
    <div class="row">
        <div class=" col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/links/editar_pendiente_vista" method="POST">
                    <div class="from-group">
                        <input type="hidden" value="{{id_pendiente}}">
                    </div>
                    <div class="form-group">
                        <input type="text" value = "{{nombre_pendiente}}" class="form-control" name="usertag" placeholder="usertag">
                    </div>
                    <div class="form-group">
                        <input type="text" value = "{{desc_pendiente}}" class="form-control" name="contra" placeholder="contraseña">
                    </div>
                    {{/each}}
                    <div class="form-group">
                            <select class="form-select btn btn-secondary" name="clase" aria-label="Default select example">
                                    {{#each clases}}
                                      <option value="{{nombre_clase}}">{{nombre_clase}}</option>
                                    {{/each}}
                                    
                            </select>                            
                      </div>
                    
                    <div class="form-group">
                        <input type="text" value = "{{fecha_muestra}}" class="form-control" name="xd" placeholder="nombre">
                    </div>
                    <div class="from-group">
                        <input type="hidden" name="nombre_usuario" value="{{fecha_valor}}">
                    </div>
                    <div class="form-group">
                        <button class="btn btn-success btn-block">Guardar</button>
                    </div>
                </form>
                </div>
            </div>
        </div>
    </div>
</div>
